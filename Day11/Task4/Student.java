package Day11.Task4;
/* 
Task on hash map
class Student
    name, rollNo, clg, place, branch
    Store all these
class helperClass
    HasMap StudentMap<String, StudentObj> //store stud obj inside

    addStudent()
        this help you to create student object take student details add that student object inside HashMap using student rollno as key
    searchStudent()
        take student rollNo as key using it search if stud in Map or not
    deleteStudent()
        take rollNo, del stud
    main()
        while(true){
            switch(case){
                case 1: addStudent()
                case 2: search()
                case 3: delete()
                case 4: exit()
            }
        }
 */
public class Student {
    String name, college, place, branch;
}
